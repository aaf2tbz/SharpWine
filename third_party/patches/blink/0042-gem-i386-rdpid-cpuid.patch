--- a/blink/cpuid.c
+++ b/blink/cpuid.c
@@ -191,6 +191,7 @@
           if (legacy) {
             bx = (1u << 3) | (1u << 5) | (1u << 8) | (1u << 9) |
                  (1u << 19);  // bmi1, avx2, bmi2, erms, adx
+            cx = 1u << 22;    // rdpid
             break;
           }
           bx |= 1 << 0;   // fsgsbase
