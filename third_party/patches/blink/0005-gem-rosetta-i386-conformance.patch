--- a/blink/alu.c
+++ b/blink/alu.c
@@ -253 +253,2 @@
-  af = cf = !!x;
+  cf = !!x;
+  af = !!(x & 15);
@@ -263 +264,2 @@
-  af = cf = !!x;
+  cf = !!x;
+  af = !!(x & 15);
@@ -273 +275,2 @@
-  af = cf = !!x;
+  cf = !!x;
+  af = !!(x & 15);
@@ -298 +301 @@
-  af = (z & 15) < (y & 15);
+  af = (z & 15) < (x & 15);
@@ -308 +311 @@
-  af = (z & 15) < (y & 15);
+  af = (z & 15) < (x & 15);
@@ -329 +332 @@
-  af = (z & 15) < (y & 15);
+  af = (z & 15) < (x & 15);
@@ -677 +680,2 @@
-  af = cf = !!x;
+  cf = !!x;
+  af = !!(x & 15);
@@ -689 +693 @@
-  af = (z & 15) < (y & 15);
+  af = (z & 15) < (x & 15);
--- a/blink/cmpxchg.c
+++ b/blink/cmpxchg.c
@@ -38 +38 @@
-  Sub8(m, Get8(m->ax), Little8(ax));
+  Sub8(m, Little8(ax), Get8(m->ax));
@@ -44,2 +44 @@
-  int c;
-  u32 x, z;
+  u32 x;
@@ -54,3 +53 @@
-    z = Get32(m->ax) - Little32(x);
-    c = Get32(m->ax) < z;
-    m->flags = (m->flags & ~(CF | ZF)) | c << FLAGS_CF | !z << FLAGS_ZF;
+    Sub32(m, Little32(x), Get32(m->ax));
@@ -84 +81 @@
-      Sub64(m, Get64(m->ax), Little64(x));
+      Sub64(m, Little64(x), Get64(m->ax));
@@ -92 +89 @@
-      Sub64(m, Get64(m->ax), x);
+      Sub64(m, x, Get64(m->ax));
@@ -101 +98 @@
-      Sub64(m, Get64(m->ax), x);
+      Sub64(m, x, Get64(m->ax));
@@ -116 +113 @@
-      Sub32(m, Get32(m->ax), Little32(ax));
+      Sub32(m, Little32(ax), Get32(m->ax));
@@ -121 +118 @@
-      Sub32(m, Get32(m->ax), x);
+      Sub32(m, x, Get32(m->ax));
@@ -140 +137 @@
-      Sub16(m, Get16(m->ax), Little16(ax));
+      Sub16(m, Little16(ax), Get16(m->ax));
@@ -145 +142 @@
-      Sub16(m, Get16(m->ax), x);
+      Sub16(m, x, Get16(m->ax));
--- a/blink/gem_embed.c
+++ b/blink/gem_embed.c
@@ -24,0 +25 @@
+#include "blink/bus.h"
@@ -69,0 +71 @@
+  InitBus();
--- a/blink/machine.c
+++ b/blink/machine.c
@@ -1073 +1073 @@
-  u64 r = x ? bsr(x) : bits;
+  u64 r = x ? bits - 1 - bsr(x) : bits;
@@ -2080,0 +2081,8 @@
+      XLAT(0x238, OpSsePminsb);
+      XLAT(0x239, OpSsePminsd);
+      XLAT(0x23a, OpSsePminuw);
+      XLAT(0x23b, OpSsePminud);
+      XLAT(0x23c, OpSsePmaxsb);
+      XLAT(0x23d, OpSsePmaxsd);
+      XLAT(0x23e, OpSsePmaxuw);
+      XLAT(0x23f, OpSsePmaxud);
--- a/blink/machine.h
+++ b/blink/machine.h
@@ -660,0 +661,8 @@
+void OpSsePmaxsb(P);
+void OpSsePmaxsd(P);
+void OpSsePmaxud(P);
+void OpSsePmaxuw(P);
+void OpSsePminsb(P);
+void OpSsePminsd(P);
+void OpSsePminud(P);
+void OpSsePminuw(P);
--- a/blink/name.c
+++ b/blink/name.c
@@ -552,0 +553,8 @@
+    XLAT(0x238, "OpSsePminsb");
+    XLAT(0x239, "OpSsePminsd");
+    XLAT(0x23a, "OpSsePminuw");
+    XLAT(0x23b, "OpSsePminud");
+    XLAT(0x23c, "OpSsePmaxsb");
+    XLAT(0x23d, "OpSsePmaxsd");
+    XLAT(0x23e, "OpSsePmaxuw");
+    XLAT(0x23f, "OpSsePmaxud");
--- a/blink/sse.c
+++ b/blink/sse.c
@@ -1340,0 +1341,11 @@
+}
+
+static void SsePminsd(u8 x[16], const u8 y[16]) {
+  unsigned i;
+  i32 X[4], Y[4];
+  memcpy(X, x, 16);
+  memcpy(Y, y, 16);
+  for (i = 0; i < 4; ++i) {
+    X[i] = MIN(X[i], Y[i]);
+  }
+  memcpy(x, X, 16);
@@ -1342,0 +1354,6 @@
+static void MmxPminsd(u8 x[8], const u8 y[8]) {
+  (void)x;
+  (void)y;
+  OpUdImpl(g_machine);
+}
+
@@ -1570,0 +1588 @@
+void OpSsePminsd(P) { OpSse(A, MmxPminsd, SsePminsd); }
